# Direct dependencies. The exact versions the image is built from are in
# requirements.lock; regenerate it after changing this file:
#
#   python -m venv /tmp/lockenv && /tmp/lockenv/bin/pip install -r requirements.txt \
#     && /tmp/lockenv/bin/pip freeze > requirements.lock

# Web app
Flask>=3.0,<4
Flask-Session>=0.8.0,<1
# Backs the server-side session store; Flask-Session's SQL backend requires it.
Flask-SQLAlchemy>=3.1.0,<4
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9.9,<3
gunicorn>=21.2.0,<24

# Google sign-in
google-auth>=2.28.0,<3
google-auth-oauthlib>=1.2.0,<2
PyJWT>=2.8.0,<3

# ServiceNow automation
selenium>=4.18.0,<5
pyotp>=2.9.0,<3

# Airtable, OpenAI and Render API calls
requests>=2.31.0,<3

# Encrypted credential storage
cryptography>=42.0.0,<46

# .env loading in development
python-dotenv>=1.0.0,<2
